# Contributing

Read `PLAN.md` and `AGENTS.md` before changing scope. Keep gameplay usable offline, preserve typed GDScript, and run `node dev/dev.mjs check` before handoff.

Do not import proposed assets until the candidate is explicitly approved and its provenance record is complete. Do not commit credentials, local paths, generated output, or chat transcripts.
